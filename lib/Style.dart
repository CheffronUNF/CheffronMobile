import 'package:flutter/material.dart';

const blue = Color(0xFF1A529F);
const yellow = Color(0xFFE7AA4B);

const textBoxDecoration = BoxDecoration(
  borderRadius: BorderRadius.all(Radius.circular(20)),
  color: Colors.white
);